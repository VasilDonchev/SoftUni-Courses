function sumFirstLast(numbers) {
    let first = numbers[0];
    let lastIndex = numbers.lenght -1;
    let last = numbers[lastIndex];
    console.log(first + last)
}
sumFirstLast(20, 30, 40);
sumFirstLast([10, 17, 22, 33]);
sumFirstLast([11, 17, 22, 69]);
