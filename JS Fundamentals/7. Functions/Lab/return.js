function sum(a,b) {
    // console.log(a + b);
    return a + b;  //  функцията sum връща (return) резултата от а + б, но не го отпечатва на конзолата
}
//sum(3, 5);
let result = sum(3, 5); // в променливата result, връщаме резултата от функцията sum
console.log(result);
console.log(result + 9);  